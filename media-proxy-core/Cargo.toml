[package]
name = "media-proxy-core"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
jni = "0.21.1"

[dev-dependencies]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
